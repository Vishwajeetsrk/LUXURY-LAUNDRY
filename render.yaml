services:
  - type: web
    name: luxury-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install && npx prisma generate && npm run build
    startCommand: npm run start
    envVars:
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
